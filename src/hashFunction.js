export default const hashFunction = (key, value) => {
  if ht.dataStore.key exists then create a new node that set has previous nodes next
  else hashTable placeholder in the dataStore
}





hashTable = {
  {name: 'sal', next:{ name: 'abe', next: null }},
  {age: 1, next:{ age: 2, next: null }},
  {height: 1, next:{ height: 2, next: null }},
  {gender: 1, next:{gender: 4, next: null }}
}
